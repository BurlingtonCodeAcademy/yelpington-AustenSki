//This will be the page for Restaurant info 
//I need to have this to display the JSON files as on page text

function Restaurants(props) {
  console.log(props.match.url) //I will need to use this to finish up the page
    return <h1> Restaurant Information</h1>;
  }
  
  export default Restaurants;